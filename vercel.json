{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt && make prod",
  "outputDirectory": "output",
  "installCommand": "npm install --prefix=/dev/null",
  "framework": null,
  "build": {
    "env": {
      "PYTHON_VERSION": "3.8"
    }
  }
}
